doctype html
html(lang="en")
  head
    title Get Started with Image Upload
    style.
      /* Resetting margin and padding for consistent layout */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Arial', sans-serif;
        background-color: #f4f7fc;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      .container {
        background-color: #ffffff;
        margin-top: 10px;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 600px;
        text-align: center;
        flex-grow: 1; /* Ensures content area takes available space */
      }

      h1 {
        font-size: 2.2rem;
        color: #333;
        margin-bottom: 1.5rem;
      }

      p {
        font-size: 1.1rem;
        color: #555;
        margin-bottom: 2rem;
      }

      .btn {
        display: inline-block;
        padding: 1rem 2rem;
        background-color: #007bff;
        color: white;
        border-radius: 5px;
        text-decoration: none;
        font-size: 1.1rem;
        transition: background-color 0.3s;
      }

      .btn:hover {
        background-color: #0056b3;
      }

      footer {
        text-align: center;
        font-size: 0.9rem;
        margin-top: 2rem;
        width: 100%;
        background-color: #fff;
        padding: 1rem;
        border-top: 1px solid #ddd;
      }

      footer p {
        color: #888;
      }
  body
    .container
      h1 Get Started with Image Upload
      p
        | Welcome to the image upload service! To get started, click the button below to upload your images.
      a.btn(href="/form") Upload Gambar
    footer
      p © 2025 Image Upload Service
